# 20_资料库

> 本页由本地 my-mind 知识库精选同步生成；本地 Markdown 是唯一可信源。

## 同步信息

- 本地路径：`20_资料库/目录说明.md`
- 同步策略：目录页同步
- 处理状态：
- 可信状态：
- 同步时间：2026-06-12 16:47:15 +0800

---

用于保存原始资料和经过初步整理的资料笔记。

资料库中的内容默认是“资料”，不是最终观点。长期观点需要晋升到原子笔记、洞察、决策或项目上下文。

## 当前分类

- `人工智能产业/`：公司、模型、算力、产业链、商业格局等外部 AI 产业资料。
- `人工智能对话/`：人工智能对话导出、对话整理稿和上下文来源。
  - `Codex/`：与 Codex、OMX、自动化、工作流直接相关的对话资料。
- `AI产品与工具/`：工具评测、产品体验、功能拆解、平台能力说明。
- `工作流与自动化/`：任务流程、自动化方法、协作模式、操作闭环。
- `设计与视觉/`：界面、排版、视觉风格、前端设计参考资料。
- `管理与组织/`：团队管理、组织协作、领导力、流程治理。
- `写作与表达/`：写作方法、表达结构、内容框架、输出素材。
- `人文与社会/`：历史、文明、社会、哲学、文化与人的长期问题资料。

## 分类规则

- 资料库按“资料主题或来源场景”分，不按是否已吸收分。
- 同一份资料只放一个主目录；跨主题通过双向链接和元数据补足。
- 对话资料优先进入 `人工智能对话/`，不要直接放进原子笔记或洞察。
- 飞书知识库导入内容优先落到主题目录；聚合页和失败解析记录可先保留为本地索引，不直接升级为观点。

## 飞书精选同步规则

本地 `20_资料库/` 是资料正本，飞书知识库只作为手机端精选阅读层。当前阶段资料库规模还小，`20_资料库/` 默认同步到飞书精选层；后续如果资料库膨胀，再通过 `策略: 不同步`、`状态: 暂停` 或目录规则收紧范围。

建议在需要同步的资料 front matter 中加入：

```yaml
飞书同步:
  策略: 精选同步
  状态: 待同步
  飞书页面: ""
  最近同步: ""
  内容哈希: ""
```

规则：

- 目录骨架可以与飞书保持一致，内容不全量同步。
- `策略` 可取 `不同步`、`目录页同步`、`精选同步` 或 `手机待读发布`。
- 当前阶段 `20_资料库/` 默认进入同步候选；不适合手机阅读或不宜外部发布的资料必须显式标为 `不同步` 或 `暂停`。
- 自动摘要、失败解析和未核验转写可以保留在本地索引，不直接同步为飞书精选页。
- 同步运行结果写入 `85_运行记录/飞书知识库同步记录.jsonl`，飞书凭证和空间 ID 不写入资料正文。
