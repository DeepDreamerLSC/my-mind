# 飞书知识库清理记录

时间：2026-06-12 17:16

## 清理目标

删除飞书知识库 `林总工的知识库` 中已经被本地 `20_资料库` 精选镜像替代的旧目录和旧文档，只保留：

- `📱 my-mind 手机待读`
- `20_资料库精选`

## 删除范围

已删除旧根目录：

- `📰 前沿动态（精选保留）`
- `📰 前沿动态（自动摘要暂存）`
- `🧠 深度阅读`
- `🔧 实践经验`
- `📚 资源索引`
- `🧹 已纳入本地待清理`

飞书 Drive 删除目录时不会级联删除 Wiki 子节点，而是会把子节点上浮到根目录。因此本次额外执行了一轮“根节点白名单”清理：除 `📱 my-mind 手机待读` 与 `20_资料库精选` 外，其余上浮到根目录的孤儿文档均已删除。

机器可追踪删除记录见：

- `85_运行记录/飞书知识库删除记录.jsonl`

## 验证结果

根节点最终只剩：

- `📱 my-mind 手机待读`
- `20_资料库精选`

`20_资料库精选` 下的精选镜像仍保留 29 个后代节点。同步 dry-run 结果为 20 个候选全部 `跳过`，说明当前本地记录仍指向有效飞书页面，不会因本次清理触发重复创建。
