interface:
  display_name: "飞书精选发布"
  short_description: "把前台推送发布成飞书精选索引和单篇文章"
  default_prompt: "Use $feishu-publish to run prepare_openclaw_feishu_message.py for the latest my-mind frontdesk push. The script publishes or reuses the Feishu/Lark bundle, then outputs the OpenClaw message with the Feishu wiki index link instead of original source links."
policy:
  allow_implicit_invocation: true
