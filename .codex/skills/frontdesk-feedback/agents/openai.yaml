interface:
  display_name: "前台反馈"
  short_description: "入队并消费 OpenClaw 前台反馈"
  default_prompt: "Use $frontdesk-feedback to append or consume OpenClaw frontdesk feedback for my-mind."
policy:
  allow_implicit_invocation: true
